apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: {{ .Values.ollama.name }}
  name: {{ .Values.ollama.name }}
  namespace: {{ .Values.namespace }}
spec:
  ports:
    - name: {{ .Values.ollama.port | quote }}
      port: {{ .Values.ollama.port }}
      targetPort: {{ .Values.ollama.port }}
  selector:
    io.kompose.service: {{ .Values.ollama.name }}
