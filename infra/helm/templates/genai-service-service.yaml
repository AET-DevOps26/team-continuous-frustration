apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: {{ .Values.genaiService.name }}
    app: {{ .Values.genaiService.name }}
    monitoring: "true"
  name: {{ .Values.genaiService.name }}
  namespace: {{ .Values.namespace }}
spec:
  ports:
    - name: {{ .Values.genaiService.port | quote }}
      port: {{ .Values.genaiService.port }}
      targetPort: {{ .Values.genaiService.port }}
  selector:
    io.kompose.service: {{ .Values.genaiService.name }}
