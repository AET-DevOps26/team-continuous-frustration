apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: {{ .Values.authService.name }}
  name: {{ .Values.authService.name }}
  namespace: {{ .Values.namespace }}
spec:
  ports:
    - name: {{ .Values.authService.port | quote }}
      port: {{ .Values.authService.port }}
      targetPort: {{ .Values.authService.port }}
  selector:
    io.kompose.service: {{ .Values.authService.name }}
